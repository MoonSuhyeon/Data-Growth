# -*- coding: utf-8 -*-
# ↑ pip 는 로케일 인코딩으로 이 파일을 읽는다. 한국어 Windows(cp949)에서는
#   한글 주석이 UnicodeDecodeError 를 낸다 — CI(리눅스, UTF-8)에서는 안 보이고
#   개발 기계에서만 깨지는 종류다.
# 분석 파이프라인과 테스트가 쓰는 것.
#
# 지금껏 **어디에도 선언돼 있지 않았다.** backend/requirements.txt 는 앱 런타임만
# 담고 있어서, pandas·scipy 없이 그대로 설치하면 테스트가 import 에서 죽는다.
# CI 를 붙이면서 드러난 구멍이다.
#
# 버전을 고정하는 이유는 pandas 때문이다. 안 묶으면 3.x 가 설치되는데 2.x 와
# 동작이 다르다 — 같은 코드가 환경에 따라 다른 결과를 내면 테스트가 무의미해진다.
-r backend/requirements.txt

pandas==2.3.3
numpy==1.26.4
scipy==1.17.1

pytest==8.3.4
httpx==0.28.1          # TestClient
